services:
  backend:
    build:
      context: .
    ports:
      - "8060:8060"
    environment:
      - DB_URL=${DB_URL}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - SERVER_PORT=8060
networks:
  default:
      name: "intune-depot-app"
      external: true
